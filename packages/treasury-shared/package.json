{
  "name": "@optomitron/treasury-shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared interfaces and mocks for treasury contracts",
  "exports": {
    "./addresses": "./src/addresses.ts"
  },
  "scripts": {
    "compile": "hardhat compile",
    "build": "hardhat compile",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.3",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "ethers": "^6.4.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "viem": "^2"
  }
}
