{
  "name": "@optomitron/db",
  "version": "0.1.0",
  "description": "Prisma schema and database client for Wishocracy",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "prisma generate && tsc",
    "test": "echo 'No tests yet (needs DATABASE_URL)'",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "prisma": "^6.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
