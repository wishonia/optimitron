{
  "name": "@optomitron/treasury-wish",
  "version": "0.1.0",
  "private": true,
  "description": "WishToken, WishocraticTreasury, and UBIDistributor for monetary reform",
  "exports": {
    "./abi": "./src/abi.ts"
  },
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "build": "hardhat compile",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.1.3",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "ethers": "^6.4.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0",
    "@optomitron/treasury-shared": "workspace:*"
  }
}
