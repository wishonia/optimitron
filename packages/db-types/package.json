{
  "name": "@optomitron/db-types",
  "version": "0.1.0",
  "description": "Prisma-aligned DTOs and mapping helpers for Optomitron",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@optomitron/db": "workspace:*",
    "@optomitron/opg": "workspace:*",
    "@optomitron/wishocracy": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.3",
    "vitest": "^1.2.0"
  }
}
